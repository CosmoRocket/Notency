import React from 'react'
import Button from '../Button'
import './LoginButton.css'

const LoginButton = () => {
  return (
    <Button
      type="submit"
      text="Login"
      className="button"
    />
  )
}

export default LoginButton